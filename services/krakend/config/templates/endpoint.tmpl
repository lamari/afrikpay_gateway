{
  "endpoint": "{{ .Endpoint }}",
  "method": "{{ .Method }}",
  "output_encoding": "json",
  "backend": [
    {
      "url_pattern": "{{ .URLPattern }}",
      "host": ["{{ .Host }}"],
      "encoding": "json"
    }
  ]
}
